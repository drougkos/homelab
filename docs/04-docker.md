# Docker Layout

## Current Approach

Each service has its own folder under `/srv/docker`.

Example:

```txt
/srv/docker/
├── monitoring/
├── nextcloud/
├── vaultwarden/
├── minecraft/
├── adguard/ or pihole/
└── reverse-proxy/
